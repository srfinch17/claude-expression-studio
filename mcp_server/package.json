{
  "name": "expression-studio-mcp",
  "version": "0.12.0",
  "description": "MCP server + engine for the Claude Expression Studio (presence, expressions, the Studio web app, and an optional ESP32-S3 LED board renderer)",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
